{
  "manifest_version": 3,
  "name": "SnipTeX",
  "version": "0.1.0",
  "description": "Copy any part of a Claude reply as Markdown, with math kept as LaTeX.",
  "browser_specific_settings": {
    "gecko": {
      "id": "sniptex@local",
      "strict_min_version": "140.0",
      "data_collection_permissions": {
        "required": [
          "none"
        ]
      }
    }
  },
  "permissions": [
    "clipboardWrite",
    "menus",
    "storage"
  ],
  "host_permissions": [
    "https://claude.ai/*"
  ],
  "background": {
    "scripts": [
      "background.js"
    ]
  },
  "content_scripts": [
    {
      "matches": [
        "https://claude.ai/*"
      ],
      "js": [
        "vendor/turndown.js",
        "vendor/turndown-plugin-gfm.js",
        "content.js"
      ],
      "run_at": "document_idle"
    }
  ],
  "commands": {
    "copy-as-markdown": {
      "suggested_key": {
        "default": "Ctrl+Alt+C"
      },
      "description": "Copy selection as Markdown (default math format)"
    },
    "copy-as-markdown-alt": {
      "description": "Copy selection as Markdown (other math format)"
    }
  },
  "options_ui": {
    "page": "options/options.html",
    "open_in_tab": false
  }
}
